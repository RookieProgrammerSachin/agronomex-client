import { View, Text } from "react-native";
import React from "react";

/** Look for custom switch component or build myself */
export default function Switch() {
  return (
    <View>
      <Text>Switch</Text>
    </View>
  );
}
